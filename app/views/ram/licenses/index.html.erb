<% if authorize_for_with_global('licenses', 'new', true) %>
	<div class="contextual">
	<%= link_to('New License', new_license_path, :class => 'icon icon-license-add') %>
	</div>
<% end %>
<h2><%= l(:label_license_plural) %></h2>
<% if authorize_for_with_global('licenses', 'index', true) %>
<div id="licenses">
  <%= render :partial => 'ram/licenses/list' %>
  <p class="pagination"></p>
</div>
<% end %>
<% html_title(l(:title_license_plural)) -%>
